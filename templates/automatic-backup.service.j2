[Unit]
Description=Automatic backup {{ backup_name }}

[Service]
Type=simple
ExecStart=/bin/sh {{ systemd_script_target }}/automatic-backup-{{ backup_name }}.sh
User={{ systemd_user }}
Group={{ systemd_group }}
